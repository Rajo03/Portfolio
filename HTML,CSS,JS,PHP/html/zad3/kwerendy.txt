SELECT imie, nazwisko, punkty FROM klienci ORDER BY punkty DESC
LIMIT 3;
SELECT nazwa, COUNT(nazwa) FROM klienci JOIN typy ON Typy_id =
typy.id GROUP BY nazwa;
SELECT zdjecie, imie, opinia FROM klienci JOIN opinie ON
klienci.id = klienci_id WHERE Typy_id=2 OR Typy_id=3;
ALTER TABLE klienci ADD adres VARCHAR(50);